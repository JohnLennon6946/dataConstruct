1.稀疏数组
当一个二维数组很多值默认是0，就会记录很多没有意义的数据，那么我们可以把该数组转换为稀疏数组
1）第一行记录共几行几列和共有多少不同的值
2）后面每行记录数组中非零值的位置的大小

应用实例，稀疏数组和常规数组之间的转换
一.二维数组转稀疏数组的思路
1.遍历原始的二维数组，得到有效的数据的个数sum
2.根据sum就可以创建稀疏数组 sparseArr int[sum+1][3]
3.将二维数组的有效数据存入到稀疏数组
二.稀疏数组转二维数组
1.先读取稀疏数组第一行用来创建二维数组
2.在读取稀疏数组后几行的数据，并赋给原始的二维数组
